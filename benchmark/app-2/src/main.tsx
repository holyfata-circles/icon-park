import React from "react";
import ReactDOM from "react-dom/client";
import { IconAiming } from "@holyfata/icon-park";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
	<React.StrictMode>
		<div>
			<h1>2</h1>
			<IconAiming />
		</div>
	</React.StrictMode>
);
